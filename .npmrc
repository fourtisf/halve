# pnpm's minimumReleaseAge policy rejects lockfile entries published < N minutes ago (some pnpm
# builds default to 1440). The lockfile is pinned and CI installs with --frozen-lockfile, so keep
# installs deterministic on every machine instead of failing for a day after each dependency bump.
minimum-release-age=0
strict-dep-builds=false
